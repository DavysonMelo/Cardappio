import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  checkbox: {
    flexDirection: 'row',
    marginBottom: 5,
  },

  checkboxLabel: {
    marginTop: 6,
  },
});

export default styles;
