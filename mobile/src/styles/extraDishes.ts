import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  extraDishes: {
    height: 25,
    flexDirection: 'row',
    justifyContent: 'space-between',
  },

  extraDishesFont: {
    fontSize: 13,
    paddingHorizontal: 15,
    color: '#9A9A9A',
  },

  extraDishesFontPrice: {
    fontSize: 13,
    paddingHorizontal: 15,
    color: '#9A9A9A',
  },
});

export default styles;
